ServerComponent로 만들었지만 Client Component 아래에 쓰이면서 client에서 쓰이게되고 클라이언트에서 사용 못하는 모듈을 쓰면 에러 발생 가능

### interleaving Server and Client Component
Props에서 Children으로 사용되는 컴포넌트는 단순히 렌더링되는 위치를 표기하는 것이며 children 내부가 서버인지 클라이언트인지는 알지 못함.

따라서 

