```

# 환경변수 추가 명령어 
export 변수명1=변수값
export 변수명2=$PATH:$변수명1/bin   #변수로 사용시 $PATH:$~ 로 사용



# 관리자 권한으로 실행 sudo
sudo apt upgrade
sudo vi 파일명


# apt 패키지 관리자(npm 같은 놈)
sudo apt update
sudo apt install openjdk-11-jdk
sudo apt install maven


# 외부 경로로 다운받기 wget
wget https://downloads.apache.org/maven/maven-3/3.8.6/binaries/apache-maven-3.8.6-bin.tar.gz

# 압축파일 해제
tar -xvzf apache-maven-3.8.6-bin.tar.gz

# 쉘을 재로드
source ~./bashrc

# 권한부여 
chmod 777 start-java.sh   ## 내부 관리자, 내부 사용자, 기타 외부 3,5,6,7 등 점수로 표기됨
chmod +x start-java.sh

# 현재 실행 스크립트파일 위치 경로로 이동하기
cd "$(dirname "$0")"




```



```
sudo netstat -tulnp | grep :4000

```